services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: seren
      POSTGRES_PASSWORD: seren
      POSTGRES_DB: seren
    ports:
      - "5432:5432"
    volumes:
      - seren_pg:/var/lib/postgresql/data

volumes:
  seren_pg:

